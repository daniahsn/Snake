package org.cis1200.snake;

// The Food interface represents objects that can be consumed by the Snake
public interface Food {

    // Method to update the Snake when it consumes the food
    void updateSnake(Snake snake);
}
